I am expecting the following two values to be the same (_: Nat) -> Nat.
But they are not.

from left hand side:
  (y) => Nat.add1(y)
to right hand side:
  (x) => induction (x) { (_) => Nat case zero => Nat.add1(Nat.zero) case add1(prev, almost) => Nat.add1(almost.prev) }

08.error.cic:
 13 |incr(three)
 14 |
 15 |// NOTE `add(one)` is not equal to `incr` by reflection, we need to prove it.
 16 |the(Equal((Nat) -> Nat, add(one), incr), refl)
 17 |
