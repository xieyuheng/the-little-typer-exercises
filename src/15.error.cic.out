I infer the type to be:
  Vector(E, prev)
But the expected type is:
  Vector(E, prev1)

src/15.error.cic:
 37 |        //     to all type checking functions,
 38 |        //     and use `reanming` during `readback`.
 39 |        //   - I handle this by using `subst` to rename bound variables.
 40 |        (prev, equal) => tail
 41 |    }
 42 |  }